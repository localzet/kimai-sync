# Database URL for PostgreSQL
# Format: postgresql://user:password@host:port/database?schema=public
DATABASE_URL="postgresql://kimai_user:password@localhost:5432/kimai_sync?schema=public"

# Optional: Prisma query logging (for debugging)
# Options: "query", "error", "warn", "info"
# PRISMA_LOG_LEVEL=info
